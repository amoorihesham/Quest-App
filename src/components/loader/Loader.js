import React from "react";
import "./loader.css";

const Loader = () => {
  return (
    <div className="loading" id="loadScreen">
      <div className="loader">loader...</div>
    </div>
  );
};

export default Loader;
